const initialState = {
  gameOver: false,
  seconds: 0,
  flagsLeft: 10,
};

const scoreBoard = (state = initialState, action) => {
  switch (action) {
    default:
      return state;
  }
};

export default scoreBoard;
